{
  "name": "openai-api-mock",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint . --ext .js,.ts,.cjs,.mjs --fix --ignore-path .gitignore",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "tail": "wrangler tail",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@cloudflare/ai": "^1.1.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.1.3",
    "prettier": "^3.2.5",
    "wrangler": "^3.51.2"
  }
}
