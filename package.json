{
  "name": "openai-api-mock",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "tail": "wrangler tail",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@cloudflare/ai": "^1.1.0",
    "prettier": "^3.2.5",
    "wrangler": "^3.47.1"
  }
}
